import { setLocalStorage, getLocalStorage } from '../db'
import debug from 'debug'

const log = debug('test:models:db')

test('simple local storage tests', async () => {
    log('write tests for setLocalStorage and getLocalStorage')
})
